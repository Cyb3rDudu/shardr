# Runtime & releases

> **In progress** — this Operations page ships with Stage S4. It will cover
> the runtime and release runbook: the `runtime/llama.lock` lifecycle, the
> daily llama.cpp update PRs (the A → E2E gate → merge bot flow), the canary
> channel, manual rollback (`--allow-downgrade`), and the shipped bundle
> contents.

The underlying versioning policy is already documented in the repository
README (["llama.cpp versioning & runner
releases"](https://github.com/Cyb3rDudu/shardr#llamacpp-versioning--runner-releases)).
