# Troubleshooting

> **In progress** — this Operations page ships with Stage S4. It will map
> symptoms to root causes using the `E_*` classes: partial fills, refused
> imports (symlinks), runner lifecycle failures, and store corruption
> checks.

The error classes it will triage are already documented today in the [User
Guide → Error reference](../user/errors.md); the store-side checks are in the
[User Guide → CLI reference](../user/cli.md#shardhive-cas-verify).
