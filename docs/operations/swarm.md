# Swarm & sync operation

> **In progress** — this Operations page ships with Stage S4. It will cover
> swarm/sync operation: seeding defaults, `[swarm]` configuration bounds,
> upload limiting, and what "use is replication" means operationally — with
> executed commands.

The behavior it will describe is already specified in
[`docs/specs/004`](https://github.com/Cyb3rDudu/shardr/blob/main/docs/specs/004-torrent-mapping.md)
and summarized for users in the [User Guide → Swarm & seeding](../user/swarm.md).
