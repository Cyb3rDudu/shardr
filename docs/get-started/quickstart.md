# Quickstart

> **In progress** — this page ships with Stage S5 (Get Started). It will
> run the full path with freshly executed outputs: `make all`, `shardhive
> serve`, an import (local / Hugging Face / BitTorrent), `shardr run`, and
> a chat completion. Nothing here is fabricated.

For the running system today, the authoritative sources are the [Importing
guide](../user/importing.md) and the canonical specs in
[`docs/specs/`](https://github.com/Cyb3rDudu/shardr/tree/main/docs/specs).
