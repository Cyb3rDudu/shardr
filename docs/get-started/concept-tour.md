# Concept tour

> **In progress** — this page ships with Stage S5 (Get Started). The
> repository's implemented behavior is documented today in the [User
> Guide → Importing](../user/importing.md) and the canonical specs in
> [`docs/specs/`](https://github.com/Cyb3rDudu/shardr/tree/main/docs/specs).

This page will walk through the flow from source bytes to a served model:
content-addressed storage, the verifying write path, swarm synchronization,
and how references are canonicalized and served.
