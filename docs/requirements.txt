# Python deps for building the shardr documentation site.
# Pinned to the versions validated locally (Python 3.14) and in CI
# (Python 3.11) — mkdocs 1.6.1 + mkdocs-material 9.7.7 build the full
# strict tree (see .github/workflows/docs.yml).
mkdocs==1.6.1
mkdocs-material==9.7.7
