# 001 — Artifact and Manifest

Purpose: define the OCI-like artifact layout and manifest format describing a model artifact in the content-addressed store.

## Open Questions

- Manifest schema: OCI manifest compatibility or divergence?
- How are layers/config split for model weights vs metadata?
- Signature/annotation story?
