# 000 — Reference Grammar

Purpose: define the canonical reference grammar for model artifacts (`shardr.io/ns/name:tag`) — namespaces, names, tags, digests, and validation rules.

## Open Questions

- Which characters are legal in ns/name/tag, and length limits?
- Are digests part of the grammar or a resolution detail?
- Case sensitivity rules?
