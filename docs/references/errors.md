# Error inventory

> **In progress** — this References page ships with Stage S3. It will list
> every `E_*` code with its meaning and source location (grep the codebase),
> presented as a table.

The current error reference lives in the [User Guide → Error
reference](../user/errors.md), which this page will supersede as the reference
home when it lands.
