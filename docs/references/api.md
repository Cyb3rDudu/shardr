# HTTP API v1

> **In progress** — this References page ships with Stage S3, derived from
> the current code. It will enumerate every `/v1/*` endpoint with request
> and response shapes, error codes, and curl examples executed for real
> (source: `internal/api/server.go` + spec 005).

The API is already documented in full today in the [User Guide → API — HTTP
v1](../user/api.md), which this page will supersede as the reference home when
it lands.
