# Configuration reference

> **In progress** — this References page ships with Stage S3. It will cover
> the `config.toml` shape, overlay precedence, tri-state bool semantics, and
> the spec 002 §7.1 allowlist keys.

The current configuration guide lives in the [User Guide →
Configuration](../user/config.md), which this page will supersede as the
reference home when it lands. Canonical spec:
[`docs/specs/002-modelfile-and-model-contract.md`](https://github.com/Cyb3rDudu/shardr/blob/main/docs/specs/002-modelfile-and-model-contract.md).
